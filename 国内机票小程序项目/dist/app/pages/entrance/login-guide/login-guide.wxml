<view class="login-guide"></view>
<block wx:if="{{invitationCount}}">
    <!-- 邀请弹窗列表 -->
    <invitations-popup showPopup="{{showInvitations}}" canClose="{{false}}" bind:close="checkCurrentRole"></invitations-popup>
</block>
<block wx:else>
    <!-- 没有邀请，选择散客或GP散客进入 -->
    <van-overlay show="{{ true }}" bind:click="handleHideOverlay">
        <view class="overlay-wrapper">
            <view class="invitation-detail-container">
                <view class="header">
                    <image class="header-icon" src="{{modalHeaderIconUrl}}" lazy-load="true" />
                </view>
                <view class="text-center f-s-16 f-w-bold">您需要预订 公务机票？</view>
                <view class="btn outlined" bindtap="toRegister" data-scene="SK">不需要</view>
                <view class="btn primary" bindtap="toRegister" data-scene="GovernmentSK">需要</view>
                <view class="f-s-12">说明</view>
                <view class="f-s-12 m-t-10">1. 公务机票即政府采购机票，享受政府采购优惠票价</view>
                <view class="f-s-12 m-t-10">2. 适用对象：各级国家机关、事业单位和团体组织工作人员，以及使用财政性资金购买公务机票的其他人员</view>
            </view>
        </view>
    </van-overlay>
</block>
<loading wx:if="{{isLoading}}" title="{{loadingTitle}}" />